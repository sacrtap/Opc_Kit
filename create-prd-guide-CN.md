# Create PRD — 使用指南

> **专业 PRD 写作助手** — 从创建、更新到验证的完整工作流支持

📚 [返回 Opc_Kit](README-CN.md) | 🎭 [Party Mode 指南](party-mode-guide-CN.md)

---

## 概述

**create-prd** 是一个专业 PRD（产品需求文档）写作助手，提供从创建、更新到验证的完整工作流支持。

| 意图 | 功能 | 触发信号 |
|------|------|----------|
| **创建** | 创建新 PRD | "新建 PRD"、"写需求文档"、"创建产品需求" |
| **更新** | 更新已有 PRD | "更新/修改现有 PRD"、"PRD 变更"、"给现有文档加功能" |
| **验证** | 验证 PRD 完整性 | "验证/检查 PRD"、"审查需求文档完整性" |

---

## 核心特性

### 🎯 智能意图识别
自动识别用户意图（创建/更新/验证），无需手动指定工作流。

### 🔄 双模式工作流
- **Coaching 模式（默认）** — 自动推理 → 完整草稿 → 自审 → 按需交互（约 5-10 分钟）
- **Fast 模式** — 快速生成完整文档 → 反向问题补充（约 2-3 分钟）

### 🔍 严格验证机制
- **第一性原理验证** — 5+ 个根本性问题确保文档基础正确
- **逻辑完整性审查** — US↔FR 双向追溯，Tracking↔Metrics 追溯
- **边界与风险扫描** — 主动识别异常流程、边界条件、外部依赖

### 📝 12 章标准模板 + 3 章自动生成
- 固定骨架：问题描述、目标定义、目标用户、用户故事、功能流程图、功能列表、功能详情、追踪设计、未来改进、风险与依赖
- 自动生成：决策日志、术语表、假设索引

### 🌐 自动语言适配
智能检测对话语言（中/英），自动生成匹配语言的 PRD。用户显式覆盖优先级最高。

### 📊 质量评分体系
7 维量化评分（满分 100 分），含生产就绪基准线（70+ = 可交付，85+ = 优秀）。

### 🎯 进度追踪
启动时自动创建任务列表，实时更新进度（"✅ 第 1-3 章已完成。正在处理用户故事..."）。

### 💡 推荐驱动交互
每次交互提供 1-3 个经过深思熟虑的推荐方案及理由，引导决策而非填空。

### 🧠 平台生态推断
从用户输入自动推断目标平台（iOS/Android/Web/小程序/管理后台），采用置信度策略。高置信度：直接使用。中置信度：使用 + 标注。低置信度：一行确认。

### 📦 增量写入与进度追踪
PRD 生成遵循 5 批次增量写入（Ch1-3 → Ch4-5 → Ch6-7 → Ch8-10 → Ch11-13）。每批次后实时进度通知。通过**临时**检查点追踪实现会话中断恢复（完成后移除 front matter）。

> **注意**：检查点元数据（generate_progress）在生成期间存储在 YAML front matter 中，PRD 生成完成后自动移除。最终 PRD 不包含 front matter；业务元数据仅存在于 `## Metadata` 部分。

### 🎨 原型工作流约束
需求先行，原型在后。在原型设计前验证 PRD 状态，跳过 PRD 时警告返工风险。

---

## 工作流模式

### Coaching 模式（默认）

Coaching 模式提供引导式交互与智能推理：

1. **自动推理** — 分析上下文并生成完整草稿
2. **自我审查** — 识别缺口和不一致
3. **按需交互** — 仅在需要时提出澄清问题
4. **推荐驱动** — 为每个决策提供 1-3 个选项及理由

**时长**：约 5-10 分钟

### Fast 模式

跳过交互，直接生成 PRD：

1. **快速生成** — 从可用信息创建完整文档
2. **反向问题** — 列出假设和缺失项，供后续补充

**时长**：约 2-3 分钟

### 模式切换

```
# Fast 模式（跳过交互，直接生成）
fast

# Coaching 模式（按需交互，渐进精炼）
coaching
```

---

## PRD 模板结构

### 12 章固定内容

| # | 章节 | 内容 |
|---|------|------|
| 1 | 问题描述 | 核心问题 + 具体问题 + 影响范围 |
| 2 | 目标定义 | 核心目标 + 成功指标（基线/目标） |
| 3 | 目标用户 | 用户类型及使用场景，P0/P1/P2 优先级 |
| 4 | 用户故事 | US-x.x 编号，"作为…我想要…以便…"格式 |
| 5 | 功能流程图 | Mermaid 流程图，含失败/超时分支 |
| 6 | 功能列表 | F-x.x 编号，含模块、优先级、平台 |
| 7 | 功能详情 | 详细规格及验收标准 |
| 8 | 追踪设计 | 事件 + 成功指标计算方法 |
| 9 | 未来改进 | P2 功能和增强项 |
| 10 | 风险与依赖 | 技术风险 + 外部依赖 |
| 11 | 决策日志 | 关键决策及理由 |
| 12 | 术语表 + 假设 | 领域术语 + 所有 [ASSUMPTION] 标签 |

### 3 章自动生成

- **审查记录** — 第一性原理验证 + 逻辑完整性 + 边界风险
- **元数据** — 作者、状态、版本、日期、项目信息
- **变更日志** — 版本历史（更新时使用）

---

## 质量保证

### 三步审查流程

#### 第一步：第一性原理验证（5+ 个问题）

1. **用户是谁？** — 目标用户是否清晰、可描述？
2. **他们想要什么？** — 用户故事是否覆盖真实场景？
3. **为什么是现在？** — 问题紧迫性是否成立（Why Now）？
4. **为什么是你的方案？** — 差异化价值是否清晰？
5. **怎么知道做对了？** — 成功指标是否可量化、可执行？
6-10+（根据产品复杂度补充，至少 5 个问题）

#### 第二步：逻辑完整性审查

| 追溯方向 | 规则 | 要求 |
|----------|------|------|
| US→FR | 每个用户故事至少被一个功能实现 | 100% |
| FR→US | 每个功能至少解决一个用户故事 | 100% |
| Tracking→Metrics | 每个追踪事件至少服务一个成功指标 | 100% |
| Metrics→Calculation | 每个成功指标在第 8 章有对应计算方法 | 100% |
| Metrics→Problem | 每个成功指标回答"我们是否解决了痛点？" | 100% |

**断点检测**：任何无法连接到上一步的流程步骤被标记为断点。

#### 第三步：边界与风险扫描

| 类别 | 探测问题 | 示例 |
|------|----------|------|
| 异常流程 | "如果用户在 X 操作中失败怎么办？" | 保存时网络断开、存储已满 |
| 边界条件 | "最大值是多少？并发调用呢？" | 收藏上限、API 限流、数据量 |
| 外部依赖 | "哪些外部 API 还没集成？有备选吗？" | 地图 API、权限系统、分享服务 |
| 不可控因素 | "如果用户不按预期使用怎么办？" | 用户收藏 1000+ 项、分享权限滥用 |

---

## 质量评分体系

每份 PRD 按 7 个维度评分（满分 100 分）：

| 维度 | 权重 | 满分 | 描述 |
|------|------|------|------|
| 完整性 | 30% | 30 | 全部 13 章存在且已填写 |
| 追溯性 | 25% | 25 | US↔FR 1:1 映射，无孤立需求 |
| 可测试性 | 15% | 15 | 验收标准可执行、可量化 |
| 异常覆盖 | 15% | 15 | 所有外部调用含失败路径 |
| 假设覆盖 | 5% | 5 | 所有推断已标注并汇总 |
| 审查完整性 | 5% | 5 | 三步审查已执行且格式固定 |
| 产品思维 | 5% | 5 | Why Now、差异化、用户分层 |

**基准分数**：
- **70+ 分** = 可交付
- **85+ 分** = 优秀
- **< 70 分** = 需要修改后再分享

---

## 方法论

- **第一性原理**（Elon Musk / 亚里士多德）— 剥离表面假设，回归基本事实
- **逆向工作法**（Amazon/PRFAQ）— 从用户痛点出发逆向推导
- **YAGNI 原则**（XP 极限编程）— 不构建当前不需要的东西
- **MoSCoW 优先级** — Must/Should/Could/Won't — 确保 v1 只包含必须有

---

## 核心优势

| 维度 | 传统方式 | create-prd 技能 |
|------|----------|----------------|
| **结构完整性** | 依赖作者经验，容易遗漏 | 强制 12 章骨架 + 双向追溯验证 |
| **质量保证** | 无自动验证机制 | 第一性原理验证 + 边界风险扫描 |
| **效率** | 大量填空式问答 | 推理 + 按需交互，推荐驱动决策 |
| **追溯性** | 功能与需求分离 | US↔FR 双向追溯，每个功能有来源 |
| **变更管理** | 无版本记录 | 强制变更日志 + 关键更新注释 |
| **专业性** | 通用模板 | 资深 PM 视角 + 行业最佳实践 |

---

## 集成

生成的 PRD 可与常见产品开发工具集成：

- **Figma**：在 PRD 元数据中包含原型 URL（`metadata.prototype`）
- **Jira**：将功能列表导出为 Jira Epic/Story 结构（F-x.x → Story ID 映射）
- **GitHub**：在 PRD 元数据中关联相关 Issue/PR（`metadata.related-docs`）
- **Confluence**：Mermaid 流程图在 Confluence Markdown 中原生渲染
- **Notion**：PRD Markdown 可干净导入，支持表格和标题
- **分析工具**：追踪事件兼容 Amplitude、Mixpanel、Segment

---

## 跨平台兼容

技能天生具备平台无关性。它们使用自然语言指令和通用工具描述，任何 AI 编程代理都能理解和执行。完整列表见[主 README](README-CN.md#跨平台兼容)。

### 工具映射（常见平台）

| 操作 | OpenCode | Claude Code | Cursor | Codex | GitHub Copilot |
|------|----------|-------------|--------|-------|----------------|
| 文件读取 | `read` | `Read` | 内置 | `read_file` | 内置 |
| 文件创建/覆盖 | `write` | `Write` | 内置 | `write_file` | 内置 |
| 精确编辑 | `edit` | `Edit` | 内置 | `edit_file` | 内置 |
| 文件查找 | `glob` | `Glob` | 内置 | `glob` | 内置 |
| 内容搜索 | `grep` | `Grep` | 内置 | `search` | 内置 |
| 大文件处理 | `ctx_execute_file` | `Bash` | `Bash` | `run_shell` | `Bash` |
| 任务列表管理 | `todowrite` | `TodoWrite` | 内置 | 内置 | 内置 |
| 子代理调度 | `task` | `Task` | ❌ | ❌ | ❌ |

### 降级策略

当特定工具不可用时：
1. **大文件分析** → 使用 `bash`/`run_shell` 配合 `head`/`tail`/`grep` 读取关键部分
2. **子代理** → 在当前会话中顺序执行任务，每步后报告进度
3. **任务列表** → 在会话开始时以文本列出计划，逐步标记完成

---

## 信任与质量

- **验证脚本** — `scripts/validate-prd.js` 程序化检查 18+ 质量标准
- **双向追溯** — US↔FR 1:1 映射，无孤立需求
- **异常路径覆盖** — 每个 API 调用和外部依赖都有失败 + 超时分支
- **版本历史** — 每次更新强制变更日志，在 PRD 元数据中追踪
- **经验证的方法论** — Amazon PRFAQ、第一性原理、YAGNI、MoSCoW 框架

---

## 使用示例

### 场景 1：创建新功能 PRD

```
用户：/create-prd 帮我写一个用户收藏功能的 PRD

技能：
1. 深度推理：分析用户动机、商业价值、技术可行性
2. 自动推理：生成 12 章完整草稿
3. 按需交互：对关键假设进行实时确认
4. 严格验证：US↔FR 双向追溯，流程图异常检查
5. 输出：高质量可交付 PRD + 假设索引 + 质量评分
```

### 场景 2：更新已有 PRD

```
用户：/create-prd 更新 docs/specs/prd-auth.md，增加 SSO 支持

技能：
1. 读取基线 PRD
2. 分析变更范围和影响
3. 更新受影响章节，版本号递增（v1.0.0 → v1.1.0）
4. 追加变更日志条目
5. 重新验证并重新评分
```

### 场景 3：验证 PRD 完整性

```
用户：/create-prd 验证 docs/specs/prd-payment.md

技能：
1. 结构扫描：检查全部 13 章是否存在
2. 追溯检查：US↔FR 映射验证
3. 质量评分：7 维评估
4. 输出：验证报告，含 Critical/Warning/Info 项
```

---

📚 **相关链接**：[Opc_Kit 概览](README-CN.md) | [Party Mode 指南](party-mode-guide-CN.md)
